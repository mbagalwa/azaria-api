import { OrderItemAccompanimentSchema } from '#database/schema'

/** Accompagnement choisi sur une ligne de commande (nom + prix figés). */
export default class OrderItemAccompaniment extends OrderItemAccompanimentSchema {}
